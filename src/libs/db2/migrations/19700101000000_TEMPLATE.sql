-- =========================================================================
-- TEMPLATE — Copy this file and rename:
--   YYYYMMDDHHMMSS_short_description.sql
-- =========================================================================

-- migrate:up
-- Replace with your migration SQL.
-- Examples:
--   CREATE TABLE foo (id integer PRIMARY KEY);
--   ALTER TABLE bar ADD COLUMN baz text;
--   CREATE INDEX CONCURRENTLY idx_foo ON foo (id);
--
-- For non-transactional statements (e.g. CREATE INDEX CONCURRENTLY),
-- run with: npm run db:migrate -- --no-transactions
SELECT 1;

-- migrate:down
-- Rollback SQL for documentation. Not executed by the runner.
-- DROP TABLE IF EXISTS foo;
